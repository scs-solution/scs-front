import styler from "../styles/MonitoringPage.module.css";

const Footer = () => {
  return (
    <footer className={styler.footer}>
      <div className={styler.footerContents}>
        <h2 className={styler.footerTitle}>footer</h2>
      </div>
    </footer>
  );
};

export default Footer;
